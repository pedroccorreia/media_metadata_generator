
import type { Movie } from '@/lib/types';

// This file is kept for structural integrity but will not be used
// when the data source is set to Firestore.
// The new data structure is too complex to maintain manually here.
export const content: { movies: Movie[] } = {
  movies: [],
};
